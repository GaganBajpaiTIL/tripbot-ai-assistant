# ${log_type} log - ${timestamp}
${log_message}
Resource Type: ${resource_type}
Resource Name: ${resource_name}
Resource ID:   ${resource_id}
Resource ARN:  ${resource_arn}

Full Attributes:
${resource_attributes}
